# Service Planner — プロジェクト指示

このプロジェクトは「個人開発サービス企画」を構造化して `output/draft.md` に出力する**対話型エージェントシステム**です。

## 動作モード

- ユーザーが `/plan-service` を実行したら、`/.claude/commands/plan-service.md` の手順に従う
- **直接コードを書かない**。ユーザーは対話で企画を深堀りしたい
- 必ず**プランモードで開始**（Shift+Tab）

## モデルルーティング（厳守）

| タスク | モデル |
|---|---|
| 単純な要約・整形・テンプレート埋め込み | **haiku** |
| デビルズ・アドボケイト的な批判・論理破綻指摘 | **sonnet** |
| 標準的な対話・課題深堀り | **sonnet** |
| アーキテクチャ判断・複雑な抽象化 | **opus**（必要時のみ） |

## サブエージェント運用ルール

- 重い探索・調査は必ずサブエージェントに委譲（メイン文脈を汚さない）
- サブエージェントには**狭く明確な指示**を与える（曖昧な「調べて」は禁止）
- 戻り値は要約のみ受け取り、メインに保持しない

## トークン節約規律

- 各フェーズ完了時に `/compact` を促す
- 関係ない話題に切り替えるときは `/clear`
- ファイルは差分編集のみ。全文書き直し禁止
- `output/draft.md` の中間版は1ファイルのみ。バックアップ作らない

## 中間アウトプット & レジューム規律（重要）

- **各フェーズの完了時点で `template-writer` を起動し、`output/draft.md` を更新する**
- ファイル冒頭の YAML フロントマターに `service_planner_status` ブロックを置き、進捗を保存:
  ```yaml
  ---
  service_planner_status:
    last_completed_phase: 1
    phase1_truth: "（真因を1文で）"
    phase2_user: "（ターゲット像を1文で）"
    phase3_value: "（提供価値を1文で）"
    last_updated: "ISO-8601"
  ---
  ```
- セッションが切れた場合、`/plan-service` 起動時に `output/draft.md` の存在を確認し、フロントマターから完了済みフェーズを読み取って**続きから再開**する
- レジューム時は完了済みフェーズの本文セクションを再読込しない。フロントマターの要約で十分

## 出力ルール

- 最終成果物は `output/draft.md` のみ
- テンプレートは `.claude/templates/draft-template.md`
- 日本語で出力
- フェーズと出力セクションの対応:
  - **フェーズ1** → 章2（アイデアの起源）、章3（課題の整理）
  - **フェーズ2** → 章4（想定ユーザー）、章5（競合）
  - **フェーズ3** → 章6（提供価値）、章7（機能）、章8（懸念点）
  - **フェーズ4** → 章9（展開）、章10（技術）、最後に章1（サービス概要）
